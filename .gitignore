# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/

# Research models (too large for git)
models/research/

# Checkpoint weights (binary, too large for git)
checkpoints/**/*.pt

# Training logs
logs/

# Training outputs
*.log
training_*.txt
training/experiments/
training_results/

# Local benchmark / experiment artifacts
*.json.progress.jsonl
*.remote_snapshot
*_results.json
*_benchmark*.json
# ...except committed reproducibility artifacts cited by REPRODUCIBILITY.md
!artifacts/negative_memory_benchmark.json
benchmarks/results/
*.tar.gz
.hermes-lab-data/
.hermes-lab/
.hermes-codex-lab.json

# OS
.DS_Store
Thumbs.db

# Build artifacts
target/
*.local
.env

# Debug scripts and recovery files
tests/debug_*
ncpu/os/neuros/_recovered_*

# SOME generated artifacts
benchmarks/internal_trajectories/
training/internal_controller_bootstrap/
training/real_memory_proof_*/
scripts/dev/tmp_benchmark.py
docs/plans/workspace_todo.md
some/todo.md
docs/DIAGRAM_PROMPT.md
some/docs/architecture/arch_full.png
some/docs/architecture/diagrams.md
some/docs/architecture/inference_diagram.png
some/docs/architecture/diagram.svg

# Archive (legacy experiments)
archive/
artifacts/archive/
experiments/ablation_results/
outputs/
.mog_synth_memory/
nsynth/.mog_synth_memory/

# autoresearch local artifacts
.nCPU_autoresearch/
paper/generated/

# prior-net training data (regenerate via: cargo run --release --bin gen_prior_data)
nsynth/data/prior_net_train*.jsonl
training/prior_net/prior_net_v1_rejected*.pt

# nsynth learned runtime caches (per-machine state, regenerated on every run —
# never commit: they only affect solver SPEED, never correctness)
.nsynth_method_router.json
.nsynth_search_family_router.json
.nsynth_solved_programs.json
.nsynth_learned_biases.jsonl

# egdc mog transpiler test binaries (compiled fixtures, ~1.5 MB each)
egdc/mog/tests_data/
