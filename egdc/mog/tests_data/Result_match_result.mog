fn get_val(flag: i64) -> Result<i64> {
  if flag > 0 { return ok(flag); }
  return err("negative");
}

fn main() -> i64 {
  r: Result<i64> = get_val(42);
  v: i64 = match r {
    ok(x) => x,
    err(e) => 0
  };
  println(f"{v}");
  return 0;
}
