fn safe_div(a: i64, b: i64) -> Result<i64> {
  if b == 0 {
    return err("div0");
  }
  return ok(a / b);
}

fn main() -> i64 {
  r: Result<i64> = safe_div(10, 2);
  v: i64 = match r {
    ok(val) => val,
    err(msg) => 0
  };
  println(f"{v}");
  return 0;
}
