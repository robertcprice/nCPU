fn find_pos(n: i64) -> ?i64 {
  if n > 0 { return some(n); }
  return none;
}

fn main() -> i64 {
  o: ?i64 = find_pos(42);
  if o is some(v) {
    println(f"{v}");
  }
  o2: ?i64 = find_pos(-5);
  if o2 is none {
    println("none");
  }
  return 0;
}
