# packages/

Standalone companion packages that ship alongside the main `ncpu/` package.

- `metal_mlp/` — Metal-backed MLP package used by the neural execution path.

Distinct from `kernels/` (low-level compiled backends) and `ncpu/` (the primary
Python package).
