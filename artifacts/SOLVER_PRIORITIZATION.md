# Solver Prioritization

Generated 2026-04-18T17:14:44Z from a sweep of
offset=80, limit=20 with CachedTeachers misses logged.

## Near-miss clusters (k=3)

These are program families the solver attempted (top-K teacher
distillation) but never converged on. A tight cluster = shape the
system *keeps* failing on = concrete solver-work target.

```

```

Total near-miss rows: 1
