# packaging/

Distribution and deployment scaffolding (separate from the importable `ncpu/`
package and the build outputs in `dist/`).

- `DEPLOYMENT.md` — deployment guide (start here).
- `homebrew/` — Homebrew formula assets.
- `modal_run.py` — Modal cloud run entry point.
- `scripts/` — packaging/release helper scripts.
