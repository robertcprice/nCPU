# Convenience requirements for a broad local nCPU development environment.
# Preferred install path for editable work:
#   pip install -e ".[demo,model,train,dev]"
# or for the lighter flagship demo surface:
#   pip install -e ".[demo,dev]"

# Flagship demos / differentiable CPU
torch>=2.4.0
torchvision>=0.20.1
numpy
tqdm>=4.66.0

# Coprocessor / model training stack
transformers>=5.3.0
peft>=0.6.0
datasets>=2.14.0
accelerate>=0.24.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
